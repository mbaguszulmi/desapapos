<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <data>
        <variable
            name="vm"
            type="com.desapabandara.pos.ui.viewmodel.ScanPrinterViewModel" />
    </data>

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="@color/black_transparent"
        android:onClick="@{() -> vm.dismiss()}">

        <View
            android:id="@+id/bg_printer_scan"
            android:layout_width="0dp"
            android:layout_height="0dp"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            android:layout_marginTop="80dp"
            android:layout_marginBottom="80dp"
            android:layout_marginHorizontal="30dp"
            android:background="@drawable/bg_white_rounded"
            android:onClick="@{() -> void}"/>

        <TextView
            android:id="@+id/tv_title"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@string/select_printer"
            app:layout_constraintTop_toTopOf="@id/bg_printer_scan"
            app:layout_constraintStart_toStartOf="@id/bg_printer_scan"
            app:layout_constraintEnd_toEndOf="@id/bg_printer_scan"
            android:layout_marginTop="16dp"
            android:textSize="16sp"
            android:fontFamily="sans-serif-medium"
            android:textColor="@color/black_1a"/>

        <androidx.recyclerview.widget.RecyclerView
            android:id="@+id/rv_printer_devices"
            android:layout_width="0dp"
            android:layout_height="0dp"
            app:layout_constraintTop_toBottomOf="@id/tv_title"
            app:layout_constraintBottom_toBottomOf="@id/bg_printer_scan"
            app:layout_constraintStart_toStartOf="@id/bg_printer_scan"
            app:layout_constraintEnd_toEndOf="@id/bg_printer_scan"
            android:layout_marginTop="16dp"
            android:paddingBottom="16dp"
            android:clipToPadding="false"/>

    </androidx.constraintlayout.widget.ConstraintLayout>

</layout>